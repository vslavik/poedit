
MARKETING_VERSION = 3.5.1

HEADER_SEARCH_PATHS = deps/icu/macos $(inherited)

BOOST = "$(SRCROOT)/deps/boost"
#include "deps/boost/boost.xcconfig"

CODE_SIGN_IDENTITY = -

#include? "poedit.user.xcconfig"
